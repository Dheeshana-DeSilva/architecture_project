spring.application.name=backend

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/bookfair_db
spring.datasource.username=postgres
spring.datasource.password=Converter
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
# Optional: You can actually remove the line below; Hibernate 6+ detects this automatically
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false


# --- NEW EMAIL CONFIGURATION (SSL - PORT 465) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.protocol=smtps
spring.mail.username=bookfair.system@gmail.com
spring.mail.password=hyba gwtv oevb gudk

# SSL Properties
spring.mail.properties.mail.transport.protocol=smtps
spring.mail.properties.mail.smtps.auth=true
spring.mail.properties.mail.smtps.starttls.enable=true
spring.mail.properties.mail.smtps.ssl.trust=smtp.gmail.com

# TIMEOUTS (60 Seconds)
spring.mail.properties.mail.smtps.timeout=60000
spring.mail.properties.mail.smtps.connectiontimeout=60000
spring.mail.properties.mail.smtps.writetimeout=60000

spring.mail.debug=true

# JWT Configuration
app.jwtSecret=MySuperSecretKeyForTheBookFairProjectWhichShouldBeVeryLong12345!
app.jwtExpirationMs= 3000000

# ── Auth0 OIDC Configuration ──────────────────────────────────────────────
# Replace YOUR_AUTH0_DOMAIN with your Auth0 tenant domain (e.g. dev-xxxx.us.auth0.com)

# JWKS endpoint — used by the OidcTokenValidator to verify Auth0-issued access tokens
auth0.jwks-uri=https://dev-g1hdov574xyc86yf.us.auth0.com/.well-known/jwks.json

# Token issuer — must match the "iss" claim in Auth0 tokens
auth0.issuer=https://dev-g1hdov574xyc86yf.us.auth0.com/

# Default role assigned to new users auto-provisioned via Auth0 (VENDOR or ADMIN)
auth0.default-role=VENDOR